# Beta document

This is the second Markdown fixture. It is intentionally shorter than the first
one so the two files produce a different number of chunks.

## Notes

Parity checks compare the reconciled DirTarget outputs byte for byte. Any drift
in the shared Rust splitter, or in how a host encodes text, would surface here.
