[project]
name = "grepify_bindings_bench_python"
version = "0.1.0"
description = "Python side of the Grepify binding benchmarks"
requires-python = ">=3.11"
dependencies = [
    "grepify",
]

[tool.uv]
package = false

[tool.uv.sources]
grepify = { path = "../../../", editable = true }
