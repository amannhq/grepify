[workspace]

[package]
name = "files_transform"
version = "0.1.0"
edition = "2024"

[dependencies]
grepify = { path = "../../../rust/sdk/grepify" }
tokio = { version = "1", features = ["full"] }
pulldown-cmark = "0.13"
